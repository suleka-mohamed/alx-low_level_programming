#include "main.h"					/**							*the function print_alphabet - prints alphabets in lowercase							*							*Return: Zero						*/
void print_alphabet(void)
{
	int i;							for (i = 'a'; i <= 'z'; i++)
	{								_putchar(i);					}								_putchar('\n');				}
